# SESSION_START.md

Use this file as the first continuity checkpoint in every new session.

## Startup protocol (always)
1. Read this file fully. ✅
2. Open `generated/ops/mission-control.md` for live project status.
3. Open `memory/YYYY-MM-DD.md` (today + yesterday).
4. If main/direct session with Wayne, also open `MEMORY.md`.
5. Continue from the current top priority without asking Wayne to repeat context.

**⚠️ CRITICAL: Always load memory files at session start! This is required for continuity!**

## Wayne preferences (must follow)
- Keep updates simple, plain-English, and easy to scan.
- Explain technical words immediately in plain English.
- Brother/fren tone is welcome.
- Proactively send concise progress updates.
- Minimize token burn where possible.
- Follow `PLAIN_ENGLISH_GUIDE.md` for all project/status updates.

## Current operating mode
- Primary objective: launch AutoBookr and progress all backlog projects with plain-English updates.
- Messaging cadence: **change-based only** (no noisy hourly pings).
- Overnight mode: parallel background jobs run on low-cost models; morning summary at 07:45.
- Daily execution focus lives in `TODAY_FOCUS.md`.
- Main completion tracker is `MASTER_PROJECT_EXECUTION_GUIDE.md`.
- Cost mode: routine cron jobs use `openrouter/minimax/minimax-m2.5` (or equivalent low-cost fallback).

## Current known status (as of 2026-02-25)
- Legacy sprint stages are complete and archived.
- AutoBookr is now top priority and actively wired:
  - Twilio test number configured with voice/SMS webhooks.
  - Stripe test setup complete with 3 paid tiers (Starter £19, Growth £49, Pro £99).
  - Backend routes mapped for `/api/twilio/*` and `/api/stripe/webhook`.
- Backup option A (Google Drive) selected; one-time auth/setup still pending.
- Mobile dashboard publish approved by Wayne; live publish step pending.

## When starting a fresh chat
Send a short first update in this format:
1. What this means in plain English
2. What’s already complete
3. What’s next
4. Any blocker (or say “No blocker”)
5. Does Wayne need to do anything right now (Yes/No)
